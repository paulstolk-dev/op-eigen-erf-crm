# Assets die je hier moet klaarzetten

Deze bestanden staan bewust NIET in git (zie `.gitignore`). Zet ze lokaal neer
vóór je rendert:

| Bestand in `public/`   | Bron uit je merkmap                              | Gebruik              |
|------------------------|--------------------------------------------------|----------------------|
| `oe-monogram.png`      | `favicon_op_eigen_erf.png` (wit, vierkant)       | watermerk rechtsonder |
| `wordmark-wit.png`     | `websitelogotransparent.png` (witte wordmark)    | outro-logo           |
| `Carlito-Regular.ttf`  | Carlito (OFL, gratis; Google Fonts / fontsource) | tekst (regular)      |
| `Carlito-Bold.ttf`     | Carlito (OFL, gratis)                            | tekst (bold)         |

Carlito is de vrije, metric-compatible variant van Calibri — zo matchen de
video's je PDF's en slides één-op-één.
